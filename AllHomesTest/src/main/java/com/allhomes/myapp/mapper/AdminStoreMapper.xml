<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.allhomes.myapp.admin.AdminStoreDaoImp">
 	<!--=============== store 검색===================== -->
 	<!-- 스토어명으로 검색 -->
 	<select id="adminStoreSearchS_name" resultType="com.allhomes.myapp.store.StoreVO">
 		select s_no,s_num,s_name,s_pic,to_char(openingdate, 'YYYY-MM-DD') openingdate,s_id,staff_n,staff_t,staff_e,s_homepage from store 
 		where s_name LIKE '%'||#{param1}||'%'
 	</select>
 	
 	<!-- 판매자 id로 검색 -->
 	<select id="adminStoreSearchS_id" resultType="com.allhomes.myapp.store.StoreVO">
 		select s_no,s_num,s_name,s_pic,to_char(openingdate, 'YYYY-MM-DD') openingdate,s_id,staff_n,staff_t,staff_e,s_homepage from store 
 		where s_id LIKE '%'||#{param1}||'%'
 	</select>
 	
 	<!-- 담당자 연락처로 검색 -->
 	<select id="adminStoreSearchStaff_t" resultType="com.allhomes.myapp.store.StoreVO">
 		select s_no,s_num,s_name,s_pic,to_char(openingdate, 'YYYY-MM-DD') openingdate,s_id,staff_n,staff_t,staff_e,s_homepage from store 
 		where staff_t LIKE '%'||#{param1}||'%'
 	</select>
 	
 	<!-- 담당자 이메일로 검색 -->
 	<select id="adminStoreSearchStaff_e" resultType="com.allhomes.myapp.store.StoreVO">
 		select s_no,s_num,s_name,s_pic,to_char(openingdate, 'YYYY-MM-DD') openingdate,s_id,staff_n,staff_t,staff_e,s_homepage from store 
 		where staff_e LIKE '%'||#{param1}||'%'
 	</select>
</mapper>