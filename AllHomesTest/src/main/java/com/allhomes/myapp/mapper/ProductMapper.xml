<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.allhomes.myapp.store.ProductDaoImp">
  	<select id="productAllList" resultType="com.allhomes.myapp.store.ProductVO">
  		select pd_no, s_no, pd_name, price, discount, main_img, stock, status, sub_c from product order by pd_no desc
  	</select>
  	<select id="selectProduct" parameterType="int" resultType="com.allhomes.myapp.store.ProductVO">
  		select pd_no, s_no, pd_name, price, discount, main_img, stock, status, sub_c from product where pd_no=#{param1}
  	</select>
  	
  	<update id="editProduct" parameterType="com.allhomes.myapp.store.ProductVO">
  		update product set pd_no=#{pd_no}, s_no=#{s_no}, pd_name=#{pd_name}, price=#{price}, discount=#{discount},
  		main_img=#{main_img}, stock=#{stock}, status=#{status}, sub_c=#{sub_c} 
  	</update>
  	<delete id="delProduct" parameterType="int">
  		delete from product where pd_no=#{param1}
  	</delete>
  </mapper>