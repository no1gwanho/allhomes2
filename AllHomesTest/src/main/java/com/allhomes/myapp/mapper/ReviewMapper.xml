<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.allhomes.myapp.review.ReviewDaoImp">
<select id="productReviewList" parameterType="int" resultType="com.allhomes.myapp.review.ReviewVO">
	select userid, pd_no, rating, img, content, writedate, s_no from review order by writedate desc
</select>
<select id="selectOneReview" parameterType="int" resultType="com.allhomes.myapp.review.ReviewVO">
	select userid, pd_no, rating, img, content, writedate, s_no from review where pd_no=#{pd_no}
</select>
</mapper>